
import { getToken, removeToken } from '@/utils/auth'

const user = {
    state:{
        token:getToken('Token')
    },
    // getters: {
    //     token: state => state.token,
    // },
    mutations: {
        
    },
    actions:{

    }
}
export default user